
#resourceViewResolver사용할 경우 설정하는 것
spring.mvc.view.prefix=/WEB-INF/view/
spring.mvc.view.suffix=.jsp

# MySQL DB setting (mysql jdbc 라이브러리 추가)
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://hi.namoolab.com:9898/ttobagi?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.username=ttobagi
spring.datasource.password=1111

#mybatis를 활용할 때 xml을 사용할 경우 xml을 사용한다고 알려줘야함
mybatis.mapper-locations=classpath:com/ttobagi/web/dao/mapper/*.xml

# 개별 최대 크기(제한 없을시 -1)
spring.servlet.multipart.max-file-size=900MB
# 전체 최대 크기
spring.servlet.multipart.max-request-size=900MB
